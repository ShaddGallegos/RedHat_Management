---
collections:
  # Base utility collections
  - name: community.libvirt
    version: ">=1.3.0"
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: ansible.eda
    version: ">=1.4.0"

  # Core platform collections
  - name: theforeman.foreman
    version: ">=3.15.0"

  # Vendors / infrastructure
  - name: nutanix.ncp
    version: ">=1.8.0"
  - name: vmware.vmware
    version: ">=2.4.0"
  - name: vmware.vmware_rest
    version: ">=3.0.0"
  - name: kubernetes.core
    version: ">=2.4.0"
  - name: servicenow.itsm
    version: ">=1.0.0"

  # Automation Controller / AWX
  - name: awx.awx
    version: ">=22.0.0"
