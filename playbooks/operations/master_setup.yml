---
# Converted from shell script to Ansible playbook
# Generated: 2025-10-07 17:29:59
# Original script: scripts/00_master_setup.sh
- name: Master Setup Playbook - All Scripts
- hosts: localhost
  gather_facts: true
  connection: local
  tasks:
- name: Display setup start message
  ansible.builtin.debug:
  msg:
  - ========================================
  - Starting Complete LVM Automation Setup
  - ========================================
- name: Install Red Hat Collections
  ansible.builtin.import_playbook: install_redhat_collections.yml
  tags:
  - collections
  - setup
- name: Setup LVM Automation Environment
  ansible.builtin.import_playbook: setup_lvm_automation.yml
  tags:
  - environment
  - setup
- name: Display completion message
  ansible.builtin.debug:
  msg:
  - ''
  - ========================================
  - Setup Complete!
  - ========================================
  - ''
  - 'Next steps:'
  - ' 1. Configure inventory: vi inventory/hosts'
  - ' 2. Create vault: cp vault_example.yml vault.yml'
  - ' 3. Edit vault: vi vault.yml'
  - ' 4. Encrypt vault: ansible-vault encrypt vault.yml'
  - ' 5. Setup monitoring: ansible-playbook playbooks/setup_monitoring_cron.yml'
  - ' 6. Start EDA: ansible-playbook playbooks/start_eda.yml'
  - ''
  - 'To stop EDA: ansible-playbook playbooks/stop_eda.yml'
