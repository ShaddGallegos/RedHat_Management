# Environment config generated by generate_and_propagate_hosts.yml
# Add to ~/.ansible/conf/env.conf and encrypt with ansible-vault as needed

[github]
username={{ github_user }}
repo={{ github_repo }}
pat={{ github_pat }}
