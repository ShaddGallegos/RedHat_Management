# /etc/hosts generated by generate_and_propagate_hosts.yml
# Scenario: {{ scenario }}
127.0.0.1   localhost localhost.localdomain
::1         localhost ip6-localhost ip6-loopback

{% for h in hosts_entries %}
{{ h.ip }} {{ h.name }} {{ (h.name.split('\.')[0]) }}
{% endfor %}
