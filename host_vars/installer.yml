# Host-specific variables for the installer host
# Use the controller's current user when running locally
# Host-specific variables for the installer host
# Use the controller's current user when running locally

ansible_user: "{{ installer_user }}"
aap_remote_user: "{{ installer_user }}"
ansible_ssh_private_key_file: "/home/{{ installer_user }}/.ssh/id_rsa"

# Optional: explicitly set the remote connection user for idempotent runs
# ansible_connection: ssh
