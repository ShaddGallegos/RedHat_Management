---
# Generated defaults for {{ integration }} integration
api_url: "{{ api_url }}"
api_token: "{{ api_token }}"
hosts:
{% for host in host_list %}  - {{ host }}
{% endfor %}
groups:
{% for group in group_list %}  - {{ group }}
{% endfor %}
