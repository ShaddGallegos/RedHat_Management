---
# Test: infrastructure role basic functionality
- name: Test infrastructure role
  hosts: localhost
  gather_facts: false
  vars:
    infrastructure_enabled: true
  tasks:
    - name: Verify role variables
      assert:
        that:
          - infrastructure_enabled is defined
