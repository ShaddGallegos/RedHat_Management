---
# Test: ansible_dev_node_inventory_generator role basic functionality
- name: Test ansible_dev_node_inventory_generator role
  hosts: localhost
  gather_facts: false
  vars:
    inventory_generator_enabled: true
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - inventory_generator_enabled is defined
