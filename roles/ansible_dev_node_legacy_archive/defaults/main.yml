---
# Default variables for ansible_dev_node_legacy_archive role
legacy_enabled: false
legacy_version: "1.0"

# Legacy ansible_dev_node_support configuration
support_legacy_playbooks: false
legacy_compatibility_mode: false

# Feature toggles
legacy_validate_compatibility: true
