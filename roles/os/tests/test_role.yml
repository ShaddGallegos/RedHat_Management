---
# Test: os role basic functionality
- name: Test os role
  hosts: localhost
  gather_facts: false
  vars:
    os_enabled: true
  tasks:
    - name: Verify role variables
      assert:
        that:
          - os_enabled is defined
