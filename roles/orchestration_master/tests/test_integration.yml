---
# Test: orchestration_master role integration
- name: Test orchestration_master integration
  hosts: localhost
  gather_facts: false
  vars:
    orchestration_master_enabled: true
  tasks:
    - name: Verify role dependencies
      assert:
        that:
          - orchestration_master_enabled is defined
        fail_msg: "Role integration failed"
