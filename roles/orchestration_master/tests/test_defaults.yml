---
# Test: orchestration_master role variable defaults
- name: Test orchestration_master defaults
  hosts: localhost
  gather_facts: false
  roles:
    - orchestration_master
  tasks:
    - name: Verify defaults loaded
      assert:
        that:
          - orchestration_master_enabled is defined
        fail_msg: "Defaults not loaded"
