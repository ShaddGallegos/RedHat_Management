---
# Test: aap_deploy role basic functionality
- name: Test aap_deploy role
  hosts: localhost
  gather_facts: false
  vars:
    rhis_aap_deployment_enabled: true
  tasks:
    - name: Verify role variables
      assert:
        that:
          - rhis_aap_deployment_enabled is defined
