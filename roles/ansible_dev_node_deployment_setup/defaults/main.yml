---
# Default variables for ansible_dev_node_deployment_setup role
deployment_setup_enabled: true
deployment_setup_version: "1.0"

# Directory structure
deployment_setup_directories:
  - "/opt"
  - "/opt/reports"
  - "/opt/inventory"
  - "/var/log"

# Feature toggles
deployment_setup_validate_environment: true
