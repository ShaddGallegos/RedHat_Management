---
# Test: integration_generic role basic functionality
- name: Test integration_generic role
  hosts: localhost
  gather_facts: false
  vars:
    integration_generic_integration_enabled: true
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - integration_generic_integration_enabled is defined
