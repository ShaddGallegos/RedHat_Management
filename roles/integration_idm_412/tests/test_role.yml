---
# Test: idm_integration role basic functionality
- name: Test idm_integration role
  hosts: localhost
  gather_facts: false
  vars:
    integration_idm_412_idm_integration_enabled: true
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - integration_idm_412_idm_integration_enabled is defined
