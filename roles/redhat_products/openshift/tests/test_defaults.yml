---
# Test: redhat_products/openshift defaults
- name: Test OpenShift defaults
  hosts: localhost
  gather_facts: false
  roles:
    - redhat_products/openshift
  tasks:
    - name: Verify defaults loaded
      assert:
        that:
          - openshift_enabled is defined
