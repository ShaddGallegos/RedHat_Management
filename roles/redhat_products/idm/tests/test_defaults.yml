---
# Test: redhat_products/idm defaults
- name: Test IdM defaults
  hosts: localhost
  gather_facts: false
  roles:
    - redhat_products/idm
  tasks:
    - name: Verify defaults loaded
      assert:
        that:
          - idm_enabled is defined
