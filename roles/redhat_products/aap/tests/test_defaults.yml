---
# Test: redhat_products/aap defaults
- name: Test AAP defaults
  hosts: localhost
  gather_facts: false
  roles:
    - redhat_products/aap
  tasks:
    - name: Verify defaults loaded
      assert:
        that:
          - aap_enabled is defined
