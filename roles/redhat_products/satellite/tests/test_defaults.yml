---
# Test: redhat_products/satellite defaults
- name: Test Satellite defaults
  hosts: localhost
  gather_facts: false
  roles:
    - redhat_products/satellite
  tasks:
    - name: Verify defaults loaded
      assert:
        that:
          - satellite_enabled is defined
