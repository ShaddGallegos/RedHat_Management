---
# Test: scenario_aap_credentials role
- name: Test scenario_aap_credentials role
  hosts: localhost
  gather_facts: false
  vars:
    aap_credentials_config_enabled: true
    aap_url: "https://aap.example.com"
    aap_username: "admin"
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - aap_credentials_config_enabled is defined
          - aap_url is defined
          - aap_username is defined
