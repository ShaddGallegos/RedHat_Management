---
# Test: deployment_setup role basic functionality
- name: Test deployment_setup role
  hosts: localhost
  gather_facts: false
  vars:
    deployment_setup_enabled: true
  tasks:
    - name: Verify role variables
      assert:
        that:
          - deployment_setup_enabled is defined
