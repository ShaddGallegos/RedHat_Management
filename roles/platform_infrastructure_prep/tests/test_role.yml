---
# Test: platform_infrastructure_prep role basic functionality
- name: Test platform_infrastructure_prep role
  hosts: localhost
  gather_facts: false
  vars:
    platform_infrastructure_prep_infrastructure_prep_enabled: true
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - platform_infrastructure_prep_infrastructure_prep_enabled is defined
