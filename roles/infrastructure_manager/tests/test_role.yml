---
# Test: infrastructure_manager role basic functionality
- name: Test infrastructure_manager role
  hosts: localhost
  gather_facts: false
  vars:
    infrastructure_manager_enabled: true
    deployment_platform: "libvirt"
  tasks:
    - name: Verify role variables
      assert:
        that:
          - infrastructure_manager_enabled is defined
          - deployment_platform is defined
