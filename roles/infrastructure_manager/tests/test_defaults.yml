---
# Test: infrastructure_manager defaults
- name: Test infrastructure_manager defaults
  hosts: localhost
  gather_facts: false
  roles:
    - infrastructure_manager
  tasks:
    - name: Verify defaults loaded
      assert:
        that:
          - infrastructure_manager_enabled is defined
