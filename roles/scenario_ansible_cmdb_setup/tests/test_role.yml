---
# Test: scenario_ansible_cmdb_setup role basic functionality
- name: Test scenario_ansible_cmdb_setup role
  hosts: localhost
  gather_facts: false
  vars:
    ansible_cmdb_setup_enabled: true
  tasks:
    - name: Verify role variables
      assert:
        that:
          - ansible_cmdb_setup_enabled is defined
