---
# Variables for aap_deploy role
# These are set at runtime and should not be modified

# This file is intentionally minimal - most configuration
# should come from defaults/main.yml or be passed at playbook runtime
