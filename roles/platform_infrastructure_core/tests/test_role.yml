---
# Test: platform_infrastructure_core role basic functionality
- name: Test platform_infrastructure_core role
  hosts: localhost
  gather_facts: false
  vars:
    platform_infrastructure_core_infrastructure_enabled: true
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - platform_infrastructure_core_infrastructure_enabled is defined
