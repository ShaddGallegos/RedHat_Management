---
# Test: cmdb role basic functionality
- name: Test cmdb role
  hosts: localhost
  gather_facts: false
  vars:
    cmdb_enabled: true
  tasks:
    - name: Verify role variables
      assert:
        that:
          - cmdb_enabled is defined
