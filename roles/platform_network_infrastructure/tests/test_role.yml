---
# Test the platform_network_infrastructure role
- name: Test platform_network_infrastructure Role
  hosts: localhost
  gather_facts: true
  
  tasks:
    - name: Run platform_network_infrastructure role
      include_role:
        name: platform_network_infrastructure

    - name: Test role completed successfully
      ansible.builtin.debug:
        msg: "✓ platform_network_infrastructure role test completed successfully"
