---
# Test: support role basic functionality
- name: Test support role
  hosts: localhost
  gather_facts: false
  vars:
    support_enabled: true
  tasks:
    - name: Verify role variables
      assert:
        that:
          - support_enabled is defined
