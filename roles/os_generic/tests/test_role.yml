---
# Test: os_generic role basic functionality
- name: Test os_generic role
  hosts: localhost
  gather_facts: false
  vars:
    os_enabled: true
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - os_enabled is defined
