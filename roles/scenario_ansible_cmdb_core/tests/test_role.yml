---
# Test: scenario_ansible_cmdb_core role basic functionality
- name: Test scenario_ansible_cmdb_core role
  hosts: localhost
  gather_facts: false
  vars:
    scenario_ansible_cmdb_core_cmdb_enabled: true
  tasks:
    - name: Verify role variables
      ansible.builtin.assert:
        that:
          - scenario_ansible_cmdb_core_cmdb_enabled is defined
