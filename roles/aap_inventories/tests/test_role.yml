---
# Test: aap_inventories_config role
- name: Test aap_inventories_config role
  hosts: localhost
  gather_facts: false
  vars:
    aap_inventories_config_enabled: true
    aap_url: "https://aap.example.com"
    aap_username: "admin"
  tasks:
    - name: Verify role variables
      assert:
        that:
          - aap_inventories_config_enabled is defined
          - aap_url is defined
